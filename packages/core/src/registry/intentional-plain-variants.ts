// Keys are `${blockId}::${variantId}`; entries opt a variant out of the registerBlock
// implementation guard without requiring a baseStyle delta or a decorate hook.
export const INTENTIONAL_PLAIN_VARIANTS: Set<string> = new Set();
