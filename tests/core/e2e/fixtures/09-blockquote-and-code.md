## Blockquote and Code Block

> This is a blockquote with some **bold** text.
>
> A second line in the same blockquote.

Here is some inline `code snippet` within a paragraph.

```javascript
function hello(name) {
  return `Hello, ${name}!`;
}
```

Paragraph after the code block.
