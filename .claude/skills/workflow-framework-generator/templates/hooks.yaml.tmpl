# Hook Specification for {{workflow_name}}
# Generated by workflow-framework-generator
#
# Platform-agnostic hook definitions.
# The deployer translates these to platform-native formats at deploy time.
# Hooks not supported by the target platform are degraded per profile.yaml.

hooks:
  PreToolUse:
    - matcher_capability: shell_exec
      script: guard_dangerous
      type: block
      description: "Block dangerous shell commands (rm -rf, git push --force, etc.)"

  PostToolUse: []

  Stop: []

  SessionStart: []
