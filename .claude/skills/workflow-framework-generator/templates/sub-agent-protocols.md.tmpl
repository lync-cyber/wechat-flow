# {{workflow_name}} — 子代理协议

## 返回值格式

所有子代理执行完成后，必须在回复末尾附加结构化结果标签:

```xml
<agent-result>
  <status>completed | needs_input | needs_revision | blocked</status>
  <outputs>
    <file>path/to/output1.md</file>
    <file>path/to/output2.md</file>
  </outputs>
  <summary>一句话描述完成的工作</summary>
</agent-result>
```

## 状态码定义

| 状态码 | 含义 | 后续动作 |
|--------|------|---------|
| completed | 任务正常完成 | orchestrator 推进到下一阶段 |
| needs_input | 需要用户提供更多信息 | orchestrator 向用户提问后以 continuation 模式重新调度 |
| needs_revision | 产出未通过质量门禁 | orchestrator 以 revision 模式重新调度同一 Agent |
| blocked | 任务无法继续 | orchestrator 标记并请求人工介入 |

## 调度类型

| 类型 | 说明 |
|------|------|
| new_creation | 首次创建新产出 |
| revision | 基于审查意见修订已有产出 |
| continuation | 从中断点继续未完成的工作 |

## 子代理约束

1. **上下文隔离**: 子代理拥有独立的上下文窗口，无法访问 orchestrator 的上下文
2. **信息传入**: 所有必要信息通过 prompt 和输入文件传入
3. **写入范围**: 子代理仅可写入其 `allowed_paths` 声明的目录
4. **工具权限**: 子代理仅可使用其 `tools` 字段声明的工具
5. **无嵌套调度**: 子代理不可调度其他子代理（除非明确授权）

## 错误恢复

### 返回值缺失
当子代理未返回 `<agent-result>` 标签时:
1. 检查目标输出目录是否有新文件产出
2. 有新文件 → 推断为 completed
3. 无新文件 → 标记为 blocked

### maxTurns 截断
当子代理被 maxTurns 限制截断时:
1. 检查是否有部分产出
2. 有实质内容的部分产出 → 以 continuation 模式重新调度
3. 无实质产出 → 标记为 blocked

### 写入违规
当子代理写入了 `allowed_paths` 以外的文件:
1. 回滚违规文件
2. 在 summary 中标注违规信息
3. 任务结果保持原状态码
