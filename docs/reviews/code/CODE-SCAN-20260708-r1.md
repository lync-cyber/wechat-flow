---
id: "code-scan-20260708-r1"
doc_type: code-review
author: reviewer
status: approved
deps: []
consumers: ["orchestrator", "tech-lead"]
---

# 项目级占位扫描报告（placeholder / TODO / stub 全量盘点）

扫描范围：`packages/*/src`、`apps/*/src`（editor / cli / mcp-server / relay / job-worker）、根 `tests/**`、`e2e/**`。扫描时点存在三个在飞实现任务（T-164/T-165/T-171），其写盘中间态已识别并排除，不计入占位。

## 总体结论

- **真占位共约 24 处，全部生产路径可达**：用户 UI 直接可触发 22 处，后端管线经过 2 处。
- **安全敏感 1 处**（MCP 鉴权直通），优先级最高。
- **纪律面良好**：生产代码零裸 `TODO`/`FIXME`/`XXX`/`HACK`；测试零 `.only` 残留、零空断言，全部 6 处 `.skip` 均为 infra-gated（Redis/Chromium 可达性 gate，合法）；全部占位均带受控标记（`placeholder: true` / `cataforge: allow(wiring_empty_handler)` / `[ASSUMPTION]`）与 backlog 追溯。

## 1. 生产可达真占位

### 1A. 命令面板空命令 ×17 — `apps/editor/src/lib/command-registry.ts`

`placeholder: true` + `run: () => {}`，经 `EditorShell.vue` `commandPaletteCommands` → CommandPalette 直调 `cmd.run()`，用户 Cmd+K 触发无任何效果：

| id | label | 备注 |
|---|---|---|
| `view-focus-mode` | 切换专注模式 (F11) | T-157 发现项⑥剩余 |
| `view-collapse-right` | 折叠右栏 | T-157 发现项⑥剩余 |
| `view-toggle-viewport` | 切换视口 | 视口按钮另有真实实现，命令未接线 |
| `view-undo` / `view-redo` | 撤销/重做 | 与 1E TopBar 空 handler 同一功能缺口 |
| `view-find` / `view-find-replace` | 查找/查找替换 | 未实现 |
| `theme-custom-color` | 自定义配色 | 未实现 |
| `doc-jump` / `doc-new` / `doc-delete` | 文档命令组 | store 已有 `createDoc`，接线成本低 |
| `content-insert-component` | 插入组件 | InsertDrawer 真实存在，命令未接线 |
| `content-zh-typo` | 中文排版修订 | 右键菜单路径真实、命令面板路径空——双路径不一致 |
| `export-copy-html` | 复制 inline HTML | TopBar `composeCopy` 真实存在，命令未接线 |
| `export-cover-landscape` / `export-cover-square` | 封面导出 | 未实现 |
| `help-whats-new` | 新功能说明 | 未实现 |

（`view-collapse-left` 已由 T-171 接线真实实现，不在上表。）

### 1B. MCP-Server 鉴权直通 ×1 — `apps/mcp-server/src/transport/http-sse.ts`（安全敏感）

默认 `passthroughResolver` 对任意/缺失 Bearer token 一律授权 `"user"` scope；生产入口 `http-entry.ts` 未注入真实 `tokenResolver`。带 `cataforge: allow(wiring_empty_handler)`，backlog E-010 / T-051。**公网暴露前必须替换。**

### 1C. Relay 管理密钥内存存储 ×1 — `apps/relay/src/admin/api-keys.ts` + `main.ts`

Admin API Key 存 per-instance 内存 Map，重启即丢，无 DB 持久化。backlog E-010。

### 1D. 设置页占位 section ×3 — `apps/editor/src/pages/SettingsPage.vue`

「主题与品牌」「同步与协作」「关于」仅占位文案，均带 `[ASSUMPTION]`，依赖主题市场/凭据同步/版本注入后续落地。同页编辑器偏好、图床、API 密钥三 section 为真实实现。

### 1E. TopBar 撤销/重做空 handler ×1 — `apps/editor/src/components/layout/EditorShell.vue`

`cataforge: allow(wiring_empty_handler)`，与 1A `view-undo`/`view-redo` 同一功能缺口的另一触发面。

### 1F. 结构重写恒等函数 ×1 — `packages/core/src/simulator/rewrite-structure.ts`

`rewriteStructure(tree)` 恒等返回，被 `simulate-paste.ts` 生产管线消费；注释已声明当前 AC 未覆盖，属已文档化最小占位。

## 2. 注释类 TODO / 测试占位

均无违规项（见总体结论纪律面）。

## 3. 处置建议（复杂度维度，不排时间）

1. **安全**：1B tokenResolver 真实化——公网暴露前置条件，随 E-010/T-051 落地（release go/no-go 关联项）。
2. **持久化**：1C 密钥 DB 化，随 E-010。
3. **编辑器核心交互**：undo/redo 一卡（1A×2 + 1E 同缺口）；查找/查找替换一卡。
4. **接线型收编**（复用既有真实实现，单点改动）：`content-zh-typo` / `content-insert-component` / `export-copy-html` / `view-toggle-viewport` / `doc-new` 把命令面板 run 接到既有 use-case，消除双路径不一致。
5. **功能型**：文档命令组（jump/delete）、封面导出组、`theme-custom-color`、`help-whats-new`、1D 三 section 随对应功能排期。
6. **低优先**：1F 待出现明确 ul/ol→table 结构重写 AC 时实现。

## 4. 排除项摘要

约 60+ 命中经上下文确认排除：HTML `placeholder` 属性 / CSS `::placeholder` 伪类（合法 UI 语义）、`resolveTokenPlaceholders` 等真实 token 逻辑、ThemeCard「即将上线」产品态、`EMPTY_REPORT` 空默认常量（真实数据由 `render.ts` 灌入）、防御性 `makeNotImplementedJobsClient` 降级默认、e2e fixture 占位串。
